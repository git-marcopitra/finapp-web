import { FC } from 'react';

import { Box, Typography } from '../../../elements';

const Header: FC = () => (
  <Box as="header">
    <Typography as="h1" m="L">
      FINapp
    </Typography>
  </Box>
);

export default Header;
